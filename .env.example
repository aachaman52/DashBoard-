PORT=3000
# Required before binding outside localhost. Use a random 32+ byte value.
CONTROL_CENTER_TOKEN=
# Optional; increases GitHub API rate limits and enables private repositories.
GITHUB_TOKEN=
GITHUB_OWNER=aachaman52
# Optional: signed-in user's Supabase JWT verification requires your Supabase project URL/key (planned).
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
